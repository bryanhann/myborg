#/usr/bin/env bash

[ "$0" == "./install" ] || echo "try './install'"
[ "$0" == "./install" ] || exit

export MYVAR=~/.local/var/myborg
mkdir -p ${MYVAR}

export xx=${MYVAR}/current.txt
touch $xx

echo "was: [$(cat ${xx})]"
echo ${PWD} > ${xx}
echo "now: [$(cat ${xx})]"

source ${PWD}/main 
