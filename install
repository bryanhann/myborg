#!/usr/bin/env bash

[ ! "$0" == "./install" ] && {
    echo "try './install'"
    exit
}

source ${PWD}/dot
echo "was: [${MYBORG}]"
echo ${PWD} > ${MYBORG_INDEX}
source ${PWD}/dot
echo "now: [${MYBORG}]"
