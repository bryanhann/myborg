#/usr/bin/env bash

[ "$0" == "./install" ] || echo "try './install'"
[ "$0" == "./install" ] || exit

export xx=~/.local/var/myborg/current.txt
echo was: $(cat $xx)
echo ${PWD} >   $xx
echo now: $(cat $xx)

source ${PWD}/main 
