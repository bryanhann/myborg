export MYBORG_CONFIG_MODULES=~/.config/myborg/modules

_clone () {
    src=$1
    dst=$2
    [ -d $dst ] || git clone $src $dst
}

_clone https://github.com/bryanhann/borg.modules.bch ${MYBORG_CONFIG_MODULES}/0.bch

