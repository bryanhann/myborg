#!/bin/bash
echo activating MODS ...
for name in $(ls -1 ${MYBORG_MODS} | grep "^[0-9]"); do
    path="${MYBORG_MODS}/$name/.insitu"
    [  -f ${path}/activate ] || continue
    echo "  activating [${name}]"
    > /dev/null pushd ${path}
    . ./activate
    > /dev/null popd
done
