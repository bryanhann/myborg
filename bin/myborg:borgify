#!/source/this/sh
_source () { echo "  sourcing [$*]"; . $*; }
main () {
    [ "$1" = "" ] && return
    echo processing BORGIFY
    _source borg:__first__
    _source ${MYBORG_DOTS}/dot$1
    _source borg:__last__
    _source borg:path:clean
}
main $*
